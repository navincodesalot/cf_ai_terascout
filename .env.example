RESEND_API_KEY=your_resend_api_key

# Optional: Tavily API for real web search (no hallucinated URLs).
# Get key at https://app.tavily.com — without it, falls back to Google News.
TAVILY_API_KEY=tvly-your_api_key
